# Core ML libraries
torch>=2.2
torchaudio>=2.2
transformers>=4.44
datasets>=3.0
evaluate>=0.4.2
accelerate>=0.33

# Specify CUDA version for PyTorch and related libraries
torch==2.3.0+cu121
torchvision==0.18.0+cu121
torchaudio==2.3.0+cu121

# Data processing and scientific computing
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5

# Model export and inference
onnx>=1.16
onnxruntime>=1.18

# Audio processing
soundfile>=0.12

# Utilities
tqdm>=4.66
matplotlib>=3.9
seaborn>=0.12
pyyaml>=6.0

# Optional: 8-bit quantization (requires compatible CUDA; official wheels primarily for Linux)
# On Windows, installation may not be available or may require workarounds.
bitsandbytes>=0.44.1; platform_system == "Linux"